#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jitsu-env/bin/jitsu-env" "$@"
  ret=$?
else 
  node  "$basedir/../jitsu-env/bin/jitsu-env" "$@"
  ret=$?
fi
exit $ret
