extends admin_layout
block content
   .container.posRel
      h2 Заявка
      hr
      | #{a.video_url}
      | #{a.concat_result}
      | #{a.user_firstname} #{a.user_lastname}
      | #{a.action} #{a.part}
